{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "target/**"]
    },
    "dev": { "persistent": true, "cache": false },
    "lint": {},
    "test": {},
    "test:integration": {
      "cache": false
    },
    "format": {
      "outputs": []
    }
  }
}
